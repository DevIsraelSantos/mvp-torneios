import { UserDto } from "./user.dto";

export type UserDetailDto = UserDto & {
  // achievements: Array<{
  // total: number;
  // oppened: number;
  // declined: number;
  // completed: number;
  // history: Array<any>;
  // }>;
  // redemptions: Array<any>
};
